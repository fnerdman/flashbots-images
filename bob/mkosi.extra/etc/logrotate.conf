# Rotate logs in /delayed_logs daily,
# keeping 5 old copies. Use copytruncate so tail
# doesn't lose its file handles.

/persistent/delayed_logs/*.log {
    daily
    rotate 5
    copytruncate
    missingok
    notifempty
    compress        
    dateformat -%Y%m%d-%H%M%S
    maxsize 2G
}
